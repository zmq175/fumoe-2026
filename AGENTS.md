# 项目代理约定

## 分支与上线（用户明确要求）

- `main` 固定作为线上生产版本分支，不作为日常开发工作区。
- 开始任何开发或修复前，先确认当前分支与工作区；必须单独创建／切换到开发分支，例如 `feat/<topic>` 或 `fix/<topic>`。禁止直接在 `main` 开发或提交功能改动。
- 若已有未提交修改停留在 `main`，应保留它们并切换到独立开发分支；不得丢弃、覆盖或擅自重置用户修改。
- 开发分支完成测试和检查后，才允许合并到 `main`；合并后再从 `main` 上线。禁止从开发分支直接部署生产。
- 合并前至少通过 `npm run check`、`npm test`、`npm run test:e2e`、`npm run build`；涉及赛制推进还须执行 `npm run test:season`，涉及数据库须在隔离环境验证迁移与历史数据不变。
- 测试通过不等于获得合并、推送或部署授权。需要用户明确要求或批准对应操作；没有授权时保留开发分支并报告验证结果。
- 发布时记录 `main` 的准确提交 SHA 与部署版本；合并至部署完成之间是待发布窗口，不能把尚未部署的提交宣称为线上版本。部署失败应明确报告，不得假称线上已对齐。
- 不擅自强推、重写历史或破坏性回滚。生产迁移前必须备份，并准备验证及恢复方案。

## 视觉与历史保护

- 遵循 [视觉风格约束](docs/visual-style-contract.md)：保留首页网格、黑青科技感及整体视觉身份，只优化布局、可读性、响应式与交互。
- 新赛季规则、素材和界面优化不得悄悄重算历史结果或覆盖历史素材版本；历史修正必须走明确的审计流程。
- 本地预览使用隔离数据，不能为了演示而修改生产数据库。
